import 'package:flutter/material.dart';

class MySpaceAndroid extends StatelessWidget {
  const MySpaceAndroid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
